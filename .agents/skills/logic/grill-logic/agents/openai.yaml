interface:
  display_name: "Grill-Logic (Human HITL Engine)"
  short_description: "Interactive sequential architectural interview walking a design tree one decision at a time via ask_question."
  default_prompt: "Interview me sequentially to align on this architectural design:"
policy:
  allow_implicit_invocation: false
