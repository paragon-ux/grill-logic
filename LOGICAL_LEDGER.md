# Project Epistemic Registry: Logical Ledger

This ledger acts as a persistent truth-maintenance registry and runtime **negative constraint firewall**. Any proposal relying on an entry marked `REJECTED` is automatically gated from execution.

---

## Active Decision Registry

| Arg ID | Premises ($P$) | Proposed Conclusion ($C$) | Status | Challenger & Evidence | Resulting Action / Contrastive Refutation Rule |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **ARG-01** | **P1**: K8s multi-node worker pods.<br>**P2**: Avoid managed DB server costs.<br>**P3**: Concurrent read/write required.<br>**P_hidden1**: SQLite WAL mode works across network hosts.<br>**P_hidden2**: NFS guarantees ACID lock & cache coherency. | **C**: Mount shared SQLite with WAL mode on an NFS ReadWriteMany (RWX) volume. | **REJECTED**<br>*(Fatal Axiom Invalidation)* | **Upstream Specs / Storage Model**: SQLite documentation confirms WAL requires host-local POSIX shared memory (`-shm`). NFS cannot synchronize memory across distinct kernels and exhibits severe `fcntl` and cache incoherency bugs leading to B-tree corruption. | **Contrastive Rule**: Do not infer shared embedded file-based storage from multi-node container environments.<br>**Derived Action ($C'$)**: Deploy in-cluster PostgreSQL single-pod with ReadWriteOnce (RWO) block volume, or use LiteFS. |
| **ARG-02** | **P1**: 600ms latency on account lookups.<br>**P2**: Redis provides sub-ms read speeds.<br>**P_hidden1**: Latency is caused by DB throughput limits.<br>**P_hidden2**: Account objects tolerate 5m staleness. | **C**: Deploy multi-node Redis cluster with 5-minute TTL cache in front of PostgreSQL. | **REJECTED**<br>*(Invalid Leap & Symptom Masking)* | **Adversarial CoT / RDBMS Baseline**: 600ms single point lookup indicates missing index (sequential table scan) or pool starvation. Redis masks root cause and introduces cache stampedes and stale permission hazards. | **Contrastive Rule**: Do not infer caching layer from database read latency without profiling query plans (`EXPLAIN ANALYZE`).<br>**Derived Action ($C'$)**: Profile query plan, add composite B-tree index, verify connection pool wait time. |
| **ARG-03** | **P1**: Windows server application.<br>**P2**: 100k concurrent connections.<br>**P3**: Zero-copy I/O required.<br>**P_hidden1**: Linux `io_uring` is supported on Windows NT.<br>**P_hidden2**: WSL2 virtualization maintains zero-copy DMA. | **C**: Re-architect network layer using Linux `io_uring` via C++ bindings. | **REJECTED**<br>*(Cross-Platform Incompatibility)* | **OS Kernel Specification**: Windows NT kernel cannot execute `io_uring` system calls. WSL2 virtualization crosses VM boundaries, destroying zero-copy DMA semantics. | **Contrastive Rule**: Do not adopt Linux-kernel subsystems (`io_uring`) on Windows.<br>**Derived Action ($C'$)**: Re-architect network layer using Windows native Winsock Registered I/O (RIO) or Windows IOCP. |
| **ARG-04** | **P1**: Monolith with 20 DB tables.<br>**P2**: 3 developers.<br>**P3**: Bi-weekly deployment bottlenecks.<br>**P_hidden**: Friction caused by monolithic packaging. | **C**: Decompose monolith into 6 gRPC microservices. | **REJECTED**<br>*(Conway's Law Inversion)* | **DORA Metrics / Fowler Prerequisites**: 0.5 devs/service and 3.3 tables/service introduces catastrophic operational overhead and distributed transaction tax without solving release batching friction. | **Contrastive Rule**: Do not infer microservice decomposition from release cycle bottlenecks on small teams.<br>**Derived Action ($C'$)**: Adopt Trunk-Based Development, automated sub-5-minute CI/CD, and Expand/Contract DB migrations. |
| **ARG-05** | **P1**: Monolith with 20 tables & 3 devs.<br>**P2**: Release friction caused by batching.<br>**P_hidden1**: Fast automated tests are achievable.<br>**P_hidden2**: Zero-downtime DB migrations are viable. | **C'**: Retain monolith; adopt Trunk-Based Development, automated CI/CD pipeline, and expand/contract migrations. | **SUPPORTED**<br>*(Validated Alternative)* | **Continuous Delivery Verification**: Low schema complexity enables sub-5-minute CI pipeline and non-blocking DDL migrations without distributed systems overhead. | **Pass to Execution**: Clear task to configure CI/CD pipeline, integration test suite, and expand/contract migration tooling. |
